# ============================================================
# Notifier dependencies (SEPARATE from the production app).
# The notify/ folder is self-contained - install these on the
# machine that sends the start/heartbeat/stop emails.
# smtplib / email / ssl are in the Python standard library.
# ============================================================

python-dotenv>=1.0   # read DPP + SMTP settings from .env
pyodbc>=5.0          # read-only DPP summary query (stop email only)
